Thumbs.db
Desktop.ini
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# 旧 Python 仮想環境（残っていれば無視）
.venv/
venv/
__pycache__/
.pytest_cache/

*.local

# WorkHelper local/user data (must never be committed)
workhelper-storage.json
app-state*.json
workhelper-backup*.json
*.workhelper-backup.json

# npm（ExcelJS の vendor 同梱用。配布 ZIP には node_modules を含めない）
node_modules/

# Electron ビルド成果物
/dist/

# Local code signing secrets (never commit private keys or passwords)
/signing-local/
*.pfx
*.p12
*.pem
*.key
*.pvk
*.spc
*.cert-password.txt
